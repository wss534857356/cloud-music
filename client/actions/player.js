
import { createAction } from 'redux-actions'

export const chooseMusic = createAction('choose music')
export const suspendMusic = createAction('suspend music')
export const dragMusicSpeed = createAction('drag music speed')
export const changeChooseType = createAction('change choose type')
export const changeSound = createAction('change sound')
export const nextMusic = createAction('next music')
